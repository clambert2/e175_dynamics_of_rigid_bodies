% Define variables and constants
Variable x, y
Constant a = 1.4 meters
Constant b = 0.7 meters

% Define equations
eq[1] = (x-5)^2/a^2 + (y-b)^2/b^2 - 1
eq[2] = (2-y)/x - 1/3

% Solve non-linear system
solve(eq = 0, x = 4, y = 1)

% Calculate the length of s1
s2 = explicit(sqrt(x^2 + (2-y)^2))
